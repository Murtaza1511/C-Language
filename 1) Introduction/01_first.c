#include <stdio.h> // stdio.h is standard library for input and output
int main() // Paranthesis tell that it is a function
/* Nothing in paranthesis indicate arguments are not taken we can also 
write void which has the same meaning */
{
    printf("********\n"); // f in printf means formatted output
    printf("********\n");
    printf("********\n");
    printf("********\n");
    return 1; // As return type of function is int we need to return int
}
// Compiler converts high level programs to binary 